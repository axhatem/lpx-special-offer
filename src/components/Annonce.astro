---
import { Image } from 'astro:assets';
import { withBase } from "../utils/config-utils.ts";
import { bannerBackgroundColor } from "../style/colors.ts";
const warningImageUrl = withBase('/images/warning.png');
---

<section>
  <Image
    src={warningImageUrl}
    alt="Warning!"
    width={20}
    height={20}
  />
  <p>Je n’accepte que 2 projets par mois.</p>
</section>

<style define:vars={{ bannerBackgroundColor }}>
 section {
  gap: 6px;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  background-color: var(--bannerBackgroundColor);
}

img {
  width: 20px;
  height: 20px;
}

p {
  color: black;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0;
  height: 2rem;
  line-height: 2rem;
  padding-bottom: 0.1rem;
}
</style>
